{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{route=\"api/leads\"}[1m])) by (status)",
          "legendFormat": "{{status}}",
          "refId": "A"
        }
      ],
      "title": "Ingest request rate by status",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "targets": [
        {
          "expr": "sum(rate(eventflow_outbox_relay_total[1m])) by (result)",
          "legendFormat": "relay {{result}}",
          "refId": "A"
        }
      ],
      "title": "Outbox relay rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "targets": [
        {
          "expr": "sum(rate(eventflow_leads_consumed_total[1m])) by (result)",
          "legendFormat": "consume {{result}}",
          "refId": "A"
        }
      ],
      "title": "Consumer ack / retry / dlq",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "targets": [
        {
          "expr": "sum(rate(http_request_errors_total{route=\"api/leads\"}[1m]))",
          "legendFormat": "ingest errors",
          "refId": "A"
        }
      ],
      "title": "Ingest error rate",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "tags": ["eventflow", "phase2"],
  "templating": { "list": [] },
  "time": { "from": "now-15m", "to": "now" },
  "title": "EventFlow Phase 2 Pipeline",
  "uid": "eventflow-phase2-pipeline",
  "version": 1
}
